This is a partial vendor of https://github.com/rockorager/libvaxis

I mostly only want a black-box libtermkey like parser, without
heavy dependencies like uucode or zigimg. 

After this experiment has been concluded, this might be worth discussing
with upstream (vaxis-parser package, with optional grapheme data)
